enum TransitionType { slideTop, slideLeft, slideRight, fade, scale, rotate }

enum NotifcationType {
  CARD_CLAIM_CREDENTIALS,
  OTP_CODE,
  NOTIFICATION_EXTERNAL_SUBMIT,
}

enum RemoteDataStatus { ideal, loading, subloading, subSuccess, loaded, error }
